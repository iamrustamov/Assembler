Создал новую переменную bler->sym, которая будет служить итератором, чтобы туда-сюда
итератор через параметры постоянно не передавать.

Неправильно пишет имя операции.
Следует уточнить - очистится ли линия, если её указатель мы будем передвигать?
Если нет, то следует копировать указатель.

? Сохраняет лейбл первой операции, однако про лейблы второй и последущих операций не запоминает.
!

Daler: "ВАЙ БЛЯЯЯЯ!"

Код операции весит 1 байт

а ещё, а ещё код типов аргументов имеется

!!! Проверить размер exec

Бехруз:
Думаю заменить argv переменную в глобальной переменной. Барои ки мефахман ки дзидем.
Барои хамин конструкцияша дигар мекнм. Будет искать по константам.
Однако настоящая проблема в том, что аргументом операций могут быть разные типы аргументов.
А чтобы решить этот вопрос - следует обратиться к битовым операциям, а именно к побитовому или.